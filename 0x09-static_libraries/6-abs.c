#include "main.h"
/**
 * _abs - it computes the absolute value of an integer.
 *
 * @n: the int to be computed
 *
 * Return: Always 0
 *
 */

int _abs(int n)

{
	if (n >= 0)
	{

		return (n);
	}

		return (-n);

}
