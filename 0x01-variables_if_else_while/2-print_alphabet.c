#include <stdio.h>
/**
 * main - it prints the alpahabet in lowercase followed by a new line
 *
 * Return: Always 0 (onyiego)
 *
 */
int main(void)
{
	char alp;

	for(alp = 'a'; alp <= 'z'; alp++)
	{
		putchar(alp);

	}

	putchar('\n';
			return (0);
			}
