#include "main.h"
/**
 * _islower - it checks for lowercase character.
 *
 * @c: The checcked character
 *
 * Return: returns 1 when if c is lowercase and 0 otherwise.
 *
 */

int _islower(int c)

{

	return (c >= 'a' && c <= 'z');
}
